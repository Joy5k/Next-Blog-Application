import React from 'react';

const page = ({ data }) => {
    console.log(data,'The is component is loaded');
    return (
        <div>
             <div className="grid lg:grid-cols-3 md:grid-cols-2 sm:grid-cols-1">
             
         </div> 
        </div>
    );
};

export default page;